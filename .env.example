# 复制为 .env 后填写；.env 不会被提交（见 .gitignore）。
# 本地默认对应 docker-compose 中的 postgres 服务。

DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/small_town_geek

# AI Provider Keys（V0.1 可留空，后续 Milestone 使用）
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
